load("@pip_deps//:requirements.bzl", "requirement")

py_binary(
    name = "llm_consumer",
    srcs = ["llm_consumer.py"],
    deps = ["//consumers/common", "//llm:llm", requirement("confluent-kafka"), requirement("python-decouple")]
)