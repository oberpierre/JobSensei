load("@pip_deps//:requirements.bzl", "requirement")

py_binary(
    name = "sourcing",
    srcs = glob(["*.py"]),
    imports = ["."],
    deps = [requirement("confluent-kafka"), requirement("pymongo")]
)